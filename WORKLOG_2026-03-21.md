# 2026-03-21 Worklog

## Summary

Today was the day the project stopped being bottlenecked by two different kinds of "almost there" state at once:

- the weaker planning-doc categories were pushed upward through real code and test coverage instead of staying as percentage estimates
- the live spawn path finally stopped ignoring the newly authored encounter slices and started routing into at least one real named encounter instead of only the older generic regional profiles

The most important shift was not just "more content exists."
It was that the authored prototype/export content and the native live spawn path started reconnecting again.

## What Changed

- expanded the lower-coverage planning-doc categories with more authored early-game content, stricter story/world-tier gates, blueprint-aware crafting routes, named-weapon progression, extra ammo/crafting lines, and more world-route density
- widened the first-10-hours script so the opening flow now has a dead-courier step, a named-elite midpoint, a war-room upgrade push, and better chapter granularity
- added new authored encounter surfaces such as `Dead Courier` / `Blackwater Courier Reclaimers`, `Rusk, The Collector's Dog`, and the stronger Silas-prep route bridge
- exposed the new authored routes through prototype UI/export/test coverage so `Crafting`, `Encyclopedia`, `World Route`, and `Story Chapter` surfaces all reflect the new data
- rebuilt and reinstalled the current live package into the real RDR2 folder with fresh backups and hash-match confirmation
- diagnosed from the user's screenshot plus live logs that the export data already contained `Rusk`, `Dead Courier`, and blueprint content, but the native runtime was still selecting only the older generic regional spawn table
- patched the native live spawn router so field spawns can choose more contextual/authored profiles by progression and so full-ledger selected entries can queue a linked encounter path instead of relying only on the boss archive route
- confirmed from the newest live log that the corrected build tag `2026-03-21-authored-spawn-pass` is loading and that the runtime now spawns `Rusk, The Collector's Dog` instead of only `Blackwater Mercenary Patrol`

## Live Result

The clearest before/after difference today:

Before the final native patch:
- the generated ledger/export already knew about `Dead Courier`, `Rusk`, blueprint routes, and new world routes
- the live runtime still spawned only older generic regional encounters like `Blackwater Mercenary Patrol` and `Tall Trees Tracker Pack`

After the final native patch:
- the latest live run loaded `Build=2026-03-21-authored-spawn-pass`
- the live log showed `Processing regional spawn request for profile=Rusk, The Collector's Dog`
- the live run continued saving progression state with:
  - `Phase=1`
  - `TrailStage=4`
  - `EnemyKills=13`
  - `LootCount=7`
  - `RewardEarned=396`
  - `CraftedMask=1903`

## Why Today Matters

Today matters because it closed a real sync gap:

1. planning docs
2. prototype/authored data
3. exported ledger content
4. native live spawn behavior

Until today, step 2 and step 3 were ahead of step 4.
Now the live runtime has at least started consuming the authored encounter intent again.

## What Still Is Not Done

- corpse interaction labels can still look generic because the spawned encounter identity and the vanilla corpse prompt/name path are still separate systems
- the native workbench modal is still using its own hardcoded recipe list instead of fully mirroring the widened exported blueprint/crafting route set
- the newly added full-ledger selected-entry spawn route should still get one explicit live confirmation pass from the game, not only workspace validation
- boss-archive summon still needs another direct live verification pass under the newer authored-spawn build
- now that authored encounter density is wider, balance and reward pacing need another evidence-based pass instead of assuming the old values still fit

## Validation

Validated today with:

- `python -m frontier_rpg simulate`
- `python -m frontier_rpg combat --encounter encounter.rusk_collectors_dog --simulated-profile`
- `python -m frontier_rpg loot --node node.blackwater_dead_courier --simulate --simulated-profile`
- `python -m unittest discover -s tests`
- `python -m frontier_rpg ui --check`
- `.\build.ps1`
- `python .\scripts\workspace_smoke_test.py`
- `.\preflight.ps1`
- `.\install_to_game.ps1 -GameRoot E:\SteamLibrary\steamapps\common\Red Dead Redemption 2`
- live hash comparison between workspace `dist` and game-root install
- live `FrontierLedger.log`, `FrontierLedger.profile.ini`, and `FrontierLedger.session.ini` inspection

## Tomorrow Focus

Priority order for tomorrow:

1. verify the new full-ledger selected-entry spawn route live, not just the regional contextual path
2. verify boss-archive summon again on the newer authored-spawn build
3. collapse the native workbench recipe list toward the exported blueprint/crafting data so the user sees the same expanded routes in both places
4. improve corpse/ped identity presentation so authored encounters feel less like generic vanilla bodies after death
5. do a tighter balance/readability pass now that named/authored encounters are actually entering the live loop
