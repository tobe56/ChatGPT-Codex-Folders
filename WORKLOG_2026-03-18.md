# 2026-03-18 Worklog

## Summary

Today was the first serious "world-behavior conversion" pass after the 2026-03-17 foundation and live install day.
The goal was not to stop at combat notes or page summaries, but to turn the requested 3/18 targets into as much real, testable workspace behavior as possible without touching the live game folder again.

## What Landed

- added a prototype combat catalog with encounter-owned stats, resistances, weaknesses, boss phases, support pressure, and test-spawn plans
- connected combat resolution to actual system outputs such as XP, drops, individual boss defeat state, and collection progress
- added world route nodes so the prototype now supports `killed -> dropped -> picked up -> stashed` flow instead of only abstract material gain
- added stash-aware crafting so field inventory and Beecher's Hope storage both count toward recipe availability and real material consumption
- centralized gameplay-effect math for combat, loot yield, crafting efficiency, travel stamina, mounted control, and medicine efficiency
- exposed those growth effects through CLI, UI detail rows, and exported dataset text instead of leaving them buried in several separate calculations
- added a prototype English / Korean shell-level selector in the desktop UI and widened export-side translation coverage for the new route/storage strings
- regenerated native export outputs and reran the full workspace verification loop

## Requested Goals Status

### 1. Combat Materialization

Substantially advanced inside the workspace:

- encounter definitions now exist for elites and bosses
- bosses now have explicit phase windows and weakness exposure notes
- boss resolution now feeds actual progression state instead of only static archive rows
- live gameplay bonuses in the native runtime were strengthened so stats / perks / family support matter more directly

Still not finished:

- fully custom native enemy and boss spawning in the real game world
- authored native boss AI packages beyond the current tracked-runtime and bonus layer

### 2. World Farming Loop

Now partially real in the workspace:

- route nodes now resolve combat into concrete drops
- loot can be picked up and moved into Beecher's Hope stash storage
- stash materials and stash items persist in the prototype profile state
- route-node CLI inspection and route-node resolution both work

Still not finished:

- full-map authored placement for those routes
- native in-field item pickup hooks and real-world stash delivery path

### 3. Growth System Effectiveness

Now materially real in the workspace:

- growth effects are centralized and visible
- combat damage, boss damage, resist, loot yield, drop quality, travel stamina, craft cost reduction, medicine efficiency, and mounted control are all derived from actual player growth state
- crafting costs now change based on skills and perks instead of being purely static

Still not finished:

- broader native live gameplay application beyond the current stamina / weapon-wear / deadeye focus

### 4. UI Accessibility Minimum

Started cleanly:

- the desktop prototype now has English / Korean shell-level selection
- shell labels are separated from the UI code into a small localization seam
- export-side translation tables were extended for the newly added route/storage wording

Still not finished:

- full dual-language content-body coverage across the whole prototype and native dataset

### 5. Structure / Balance Cleanup

Improved today:

- new work landed under dedicated feature/data/platform modules instead of re-bloating the old monolith
- growth-effect logic now has a clearer single source instead of being implied across several paths
- route-node drops and effective recipe costs received the first practical balance pass

Still not finished:

- broader drop / recipe / reward tuning with real in-game feel checks
- native placement / runtime integration cleanup after the next live hook pass

## Verification

Validated today with:

- `python -m unittest discover -s tests`
- `python -m frontier_rpg combat`
- `python -m frontier_rpg combat --simulated-profile`
- `python -m frontier_rpg loot`
- `python -m frontier_rpg loot --node node.beechers_supply_drop --simulate`
- `python -m frontier_rpg effects --simulated-profile`
- `python -m frontier_rpg ui --check`
- `.\build.ps1`
- `python .\scripts\workspace_smoke_test.py`
- `.\preflight.ps1`

## Practical End-Of-Day State

The workspace is now beyond:

- combat definitions as pure notes
- loot gain as pure simulation shorthand
- growth effects as mostly hidden math
- locale handling as one hard-coded English path

It is still not yet at:

- full live-game enemy/boss spawn integration
- full authored world placement
- full Korean content parity

## Tomorrow Focus

Tomorrow should continue from the current `2026-03-18` workspace state, not restart from the older 3/17 plan.

Priority order:

1. native enemy/boss spawn hooks and test summon path
2. broader world-route placement and richer pickup/drop routing
3. more live gameplay hooks beyond stamina / wear / deadeye
4. Korean content-body coverage where the new route/storage/growth surfaces still read too English-first
5. first tighter balance sweep for route rewards, recipe costs, and boss payouts
