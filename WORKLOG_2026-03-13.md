# Work Log - 2026-03-13

## Today Summary

Today's work stayed centered on `ingame_mod` and pushed the project from a broad native ledger viewer toward a more real gameplay-linked mod state.

## What Was Finished Today

### 1. Native Live Run Persistence

- Added `FrontierLedger.profile.ini` as a second persistent state file beside `FrontierLedger.session.ini`.
- The ASI now saves and restores:
  - discovered region anchors
  - field travel total
  - Beecher's Hope return cycles
  - live phase

Key file:

- `E:\RDR2 RPG MODS\개발\ingame_mod\src\frontier_ledger.c`

### 2. Live Dataset Aliases

- Added `live_en` / `live_ko` dataset aliases.
- These aliases now resolve to `baseline`, `simulated`, or `endgame` native datasets based on the live run state.
- The workspace `FrontierLedger.ini` now defaults to `live_en`.

Key file:

- `E:\RDR2 RPG MODS\개발\ingame_mod\FrontierLedger.ini`

### 3. Live Objective And Detail Injection

- The native ledger now overrides important page summaries and key entry details at runtime.
- `Overview`, `Trails`, `Frontier Map`, `Homefront`, `Farming`, `Network`, `World`, and `Ending` now react to the current run state instead of only showing baked text.

Key file:

- `E:\RDR2 RPG MODS\개발\ingame_mod\src\frontier_ledger.c`

### 4. First Frontier Chain Tracking

- Added `TrailStage` to the live native profile.
- The first frontier route now advances through:
  - Ranch Briefing
  - First Field Sweep
  - First Report Returned
  - Great Plains Pressure
  - Blackwater Contract Ready
  - Silas Redd Prep
- This progression is driven by real field travel, discovered regions, and Beecher's Hope returns.
- The result is visible through the existing `Overview` and `Trails` flow and is saved/restored through the profile file.

Key file:

- `E:\RDR2 RPG MODS\개발\ingame_mod\src\frontier_ledger.c`

### 5. Workspace Smoke Validation

- Added and expanded `workspace_smoke_test.py`.
- The smoke harness now validates:
  - baseline live run attach/timeout/detach
  - seeded endgame live run attach/timeout/detach
  - `session` file output
  - `profile` file output
  - `TrailStage` persistence

Key file:

- `E:\RDR2 RPG MODS\개발\ingame_mod\scripts\workspace_smoke_test.py`

## Verification Run Today

- `python -m unittest discover -s tests`
- `.\build.ps1`
- `python .\scripts\workspace_smoke_test.py`
- `.\preflight.ps1 -GameRoot "E:\SteamLibrary\steamapps\common\Red Dead Redemption 2"`

All of the above passed in the workspace without copying a new build into the live game folder.

## Overall Progress

- Workspace native-mod package completeness: about `98%`
- Real full gameplay-overhaul completeness: about `91%`

### Why It Is Not 100%

The biggest remaining gaps are still on the true gameplay-hook side:

- real save/native progression reads
- perk/crafting/gear effects applied back into live gameplay
- world/NPC/story trigger chains that mutate the sandbox itself

## Tomorrow's Smallest Next Task

Implement one actual gameplay effect tied to the live run:

- make `TrailStage 5 / Silas Redd Prep` activate one real `World Tier 1 -> 2` gameplay change inside `ingame_mod`

That is the smallest next step that increases actual play impact instead of only tracking state.
