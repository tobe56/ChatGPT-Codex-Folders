# 2026-03-15 Worklog

## Summary

Today focused almost entirely on the native `ingame_mod` workbench flow and the Korean in-game UI around it.
The goal was not to widen the prototype, but to turn the existing crafting/status layer into something that can actually be opened, navigated, and understood inside live gameplay.

All work stayed inside `E:\RDR2 RPG MODS\개발` first, followed by repeated install / verify cycles into the real game folder only after workspace validation passed.

## What Was Built Or Pushed Forward

- confirmed the first real `Beecher's Hope` workbench interaction slice in `ingame_mod`
  - `E` confirm/open flow
  - dedicated workbench modal
  - `Craft / Archive / Result` tabs
  - session/profile persistence through `FrontierLedger.session.ini` and `FrontierLedger.profile.ini`
- connected crafted unlocks to the existing live progression and bonus layer through `CraftedMask`
- added additional representative craft items:
  - `Frontier Tonic Pack`
  - `Gunsmith Tool Roll`
  - `Deadeye Charm`
- expanded the workbench archive so it can act more like a material encyclopedia
  - salvage
  - hide bundles
  - tonic stock
  - relic fragments
- iterated multiple times on the Korean workbench renderer
  - stronger panel opacity
  - larger workbench body text
  - shorter list labels
  - Korean/ASCII mixed helper output
  - separate Korean workbench body render path

## What Was Verified

Repeatedly verified from the development workspace:

- `ingame_mod\build.ps1`
- `python -m unittest discover -s tests`
- `python -m frontier_rpg ui --check`
- `python .\scripts\workspace_smoke_test.py`
- `.\preflight.ps1`

These all passed after the latest workbench follow-up.

## Live Game Observations

The workbench modal is now:

- opening in-game
- reacting to `E`
- switching tabs
- persisting state
- showing more than the first row in live footage

But the Korean workbench UI is still not fully “done.”
The major remaining issue is not missing systems anymore.
It is the final presentation quality of the workbench body:

- some helper strings are still mixed or partially broken in Korean
- list/body scale is better, but not yet fully consistent
- some late-night iterations revealed that not all newly added Korean strings are guaranteed to render cleanly every time
- the whole window can still feel like only part of the intended information is visible

## Honest Status At End Of Day

The workbench system itself is now real enough to keep.
The main unfinished piece is the final Korean workbench presentation polish.

In practical terms:

- backend / interaction flow: in good shape
- persistence / crafted-mask linkage: in good shape
- Korean workbench readability / layout confidence: improved, but still not final

## Tomorrow's Smallest Next Tasks

1. remove the remaining duplicated/legacy Korean workbench rendering path and keep only one active renderer
2. make the workbench Korean strings use one clean helper set end-to-end
3. regenerate the bitmap/runtime string coverage specifically for the workbench labels actually shown in-game
4. re-test in development workspace
5. re-install to the game folder and check one more live video

## Refactor Audit Addendum

I also did a broader workspace/code audit today instead of treating the problem as “just another UI tweak.”

### Folder-Level Findings

- `E:\RDR2 RPG MODS\개발` is still structurally reasonable, but it has accumulated temporary video-frame folders from repeated live-capture analysis.
- those folders are not large, but they are development clutter and should be cleared in a later cleanup pass
- the real structural code hotspot remains `ingame_mod`, not the Python prototype side

### Code-Level Findings

The biggest code-quality risk is still concentrated in:

- `ingame_mod\src\frontier_ledger.c`

Specifically:

- duplicated Korean workbench rendering paths still exist in the file
- duplicated helper families exist for archive/result/current-detail generation
- `frontier_confirm_workbench_action` still carries legacy/duplicated logic after newer Korean-specific handling
- workbench strings are split across multiple helper layers, which makes later UI fixes riskier than they should be

### What Was Cleaned Today

- confirmed the workbench/Korean path is the primary refactor target
- neutralized one extra duplicate Korean render branch so the latest intended Korean workbench path is the active one
- re-ran build, tests, UI check, smoke, and preflight after that cleanup

### What Still Looks Dangerous

These are the places that I would expect to cause future problems if left alone:

1. duplicated workbench helper paths
2. duplicated Korean render branches inside the native workbench renderer
3. mixed string-generation paths for Korean labels / compact helpers / result text
4. the continued monolithic size of `frontier_ledger.c`

In short: the code is still functional, but future work will get harder unless the workbench/Korean path is collapsed into one clean flow.

## Story + Family Push

I also started the deliberate push on planning-doc categories `00` and `04`.
Instead of widening the prototype, I added more native `ingame_mod` runtime state so those two documents are represented by something the player can actually see or feel in-game.

What was added:

- Surveyor clue state
- sky-devil myth stage
- Abigail support rank
- Jack support rank
- Uncle support rank
- ranch threat / ranch defense
- farming lane recommendation

What now uses those values:

- `Homefront`
- `Farming`
- `World`
- `Ending`
- live objective text
- live bonus calculation
- live status/log messages

This matters because the story/family side is no longer only “planned.”
It now has real runtime hooks inside the native mod.

## Late Workbench Cleanup Pass

After the audit notes above, I did the next pass directly inside `ingame_mod\src\frontier_ledger.c` instead of widening scope again.

What changed:

- collapsed the active workbench overlay to one locale-aware render path instead of leaving Korean-only and stale fallback branches side by side
- collapsed the workbench confirm flow to one authoritative path instead of maintaining a separate Korean action handler
- removed dead duplicate helper logic in recipe / archive / result handling
- refreshed runtime bitmap string coverage for the Korean workbench labels and messages that the active renderer now uses

What this means by category:

- `Craft`: functionally complete, now on a cleaner single render/confirm path
- `Archive`: functionally complete, now pinned/result flow uses the same cleaned native path
- `Result`: functionally complete, now fed by the same cleaned result helper path
- renderer / helper cleanup: materially improved and now past the 70% threshold, but not yet a final live-capture signoff

What is still not fully done:

- one more real in-game readability pass after the rebuilt bitmap/glyph set
- any final spacing tweaks that only become obvious in live footage

Workspace verification after this cleanup pass:

- `ingame_mod\build.ps1`
- `python -m unittest discover -s tests`
- `python -m frontier_rpg ui --check`
- `python .\scripts\workspace_smoke_test.py`
- `.\preflight.ps1`

All passed again from the development workspace.

## End-Of-Day Recovery Note

The late Korean workbench follow-ups did not settle cleanly.
Two separate readability experiments caused regressions instead of producing a trustworthy live result:

- one pass improved renderer-path consistency but did not fully solve small Korean readability
- a later bitmap/font tuning pass overreached and damaged live workbench layout, including one near-empty/blank presentation state

Because of that, I rolled the live game folder back to the previous backup state before ending the day.
That rollback matters more than preserving a risky install.

### Live State At Stop

- real game folder: rolled back from the latest regressed install
- workspace: contains the latest investigation/fix work, but it is not treated as live-signed-off
- conclusion: do not trust tonight's late Korean live screenshots as an acceptable final state

### Root Cause Summary

The workbench problem is not a single missing label.
It is a combination of:

- mixed text-resolution paths
- Korean bitmap readability limits at small UI sizes
- fragile coupling between layout scale, glyph generation, and the workbench's compact renderer
- too much late tuning against the Korean presentation path before locking an English-stable baseline

### Direction Reset For Tomorrow

Tomorrow should not begin with "one more Korean patch."
Tomorrow should begin with an English-first rebuild of the workbench and related ledger text flow.

The new rule for tomorrow:

1. make the entire active workbench UI stable in full English first
2. keep Korean as a clean optional locale layer, not the primary implementation path
3. leave explicit seams for Korean labels/data so the locale can be swapped in later without renderer chaos
4. prefer a real locale switch or config-driven `en/ko` mode over ad-hoc mixed rendering
5. do the first live verification on the English baseline before attempting another Korean live pass

### Practical Localization Target

The desired architecture from tomorrow onward is:

- English-first UI, layout, and renderer behavior
- Korean-ready label/data hooks left in place
- either:
  - a clean `live_en` / `live_ko` option path
  - or a direct English/Korean selection option in config/runtime

The key requirement is not "Korean everywhere tomorrow."
The key requirement is that Korean can be dropped in cleanly later without breaking layout or forcing another renderer split.

### Tomorrow's First Tasks

1. freeze tonight's live rollback state and avoid new blind game-folder installs
2. audit the active workbench text/render path for English-only stability
3. rebuild the workbench list/detail/result flow with English as the authoritative baseline
4. keep Korean entry points as hooks/seams only
5. decide between:
   - config locale switch
   - `live_en` / `live_ko` dataset option
6. only after the English baseline is stable, reintroduce Korean in one controlled pass

### Handoff Note

Tomorrow can start immediately from this note.
No extra archaeology should be needed before the next pass.

The one thing I cannot honestly promise tonight is "launch the game and trust the latest install."
I rolled the live install back because the last pass regressed too far.
Tomorrow's immediate start point is therefore the cleaned handoff and the English-first rebuild plan, not a claimed final Korean live build.
