## 2026-03-13

- ingame_mod 50% migration milestone: the native export now carries `Overview`, `Active Trails`, `Frontier Map`, `Homefront`, `Gear`, `Armory`, `Ledger of the Dead`, `Collection`, and `endgame` datasets from the Python prototype instead of stopping at the original first-pass pages.
- native UI scaling for the full ledger was rebuilt for the larger page set. `frontier_ledger.c` now supports up to 24 pages and uses a sliding tab window with a current-page indicator so the ASI layout remains usable as more prototype systems move into the in-game mod.
- workspace verification for the 50% pass completed successfully with `ingame_mod\\build.ps1`, `python -m unittest discover -s tests`, `python -m frontier_rpg overview`, and `python -m frontier_rpg endgame`. A fresh package was regenerated under `ingame_mod\\dist\\FrontierLedger` without touching the live game folder.
- ingame_mod 70% migration milestone: native export now also carries `Farming`, `Network`, `World`, and `Ending`, bringing the ASI page set much closer to the full document-led prototype instead of leaving those systems inside Python-only validation.
- added export regression coverage in `tests\\test_ingame_export.py` so the native dataset must keep the expanded 18-page ledger structure, including baseline, simulated, and endgame flows for the newly migrated pages.
- verification for the 70% target pass completed with `python -m unittest discover -s tests`, `ingame_mod\\build.ps1`, `python -m frontier_rpg endgame`, generated-header inspection for the new page ids, and `preflight.ps1` against the target game root, still without copying anything into the live game folder.
- ingame_mod 90% push: added native runtime context tracking from real player coordinates. The ASI now derives a live frontier region label and route hint, feeds that into the compact/full ledger, and uses it to auto-focus region-sensitive pages like `Frontier Map`, `Farming`, `Network`, `World`, and `Ending`.
- added session persistence in the native mod. `FrontierLedger.session.ini` now stores the active dataset, last page, and selected entries per page so the ledger restores its working state between loads instead of always booting as a fresh static viewer.
- validation for the 90% pass completed with workspace-only build/test/preflight plus an isolated `LoadLibrary` smoke test that produced attach, timeout, detach, and session-file output cleanly from the built ASI without touching the live game folder.
- prototype alignment pass: `frontier_rpg` now centers the design-doc systems instead of duplicating vanilla HUD data. `Active Trails`, `Frontier Map`, `Beecher's Hope Hub`, `Homefront`, `Gear`, `Armory`, `Ledger of the Dead`, `Collection`, `Farming`, `Network`, `World State`, and `Ending` are now represented as executable prototype state, CLI output, UI state, and regression tests.
- ingame_mod live-run completion push: the ASI now maintains `FrontierLedger.profile.ini` alongside `FrontierLedger.session.ini`, so discovered frontier regions, field travel, Beecher's Hope return cycles, and the current live phase all persist between loads.
- added `live_en` and `live_ko` dataset aliases. These aliases auto-resolve to baseline, simulated, or endgame exports based on the live run state, so the in-game ledger can react to real movement and hub-return loops without copying anything into the live game folder.
- added `ingame_mod\\scripts\\workspace_smoke_test.py` and verified the built ASI in an isolated temp runtime. The smoke run now confirms attach/timeout/detach logging plus clean generation of both session and profile files before any real game-folder install.
- live-run polish pass: `Overview`, `Trails`, `Frontier Map`, `Homefront`, `Farming`, `Network`, `World`, and `Ending` now inject runtime summaries and live objective details directly inside the native ledger instead of only switching datasets behind the scenes.
- strengthened the workspace smoke harness so `live_en` is validated in both baseline and seeded-endgame states, proving that the live alias resolves to different baked exports from the same ASI without touching the live game folder.
- actual chain-tracking pass: the native live profile now persists `TrailStage` for the first frontier chain, so the opening blood-marked-ledger route advances from ranch briefing through field sweep, return report, Great Plains pressure, Blackwater contract readiness, and Silas Redd preparation based on real field travel, discovered regions, and Beecher's Hope returns.
- ingame_mod live-bridge step 1: the native profile now persists real run counters for `EnemyKills`, `LootCount`, `AnimalKills`, and `RewardEarned`, updates Overview/Farming/Network/World from those values, and folds the new action counts into TrailStage progression without adding any new pages.
- ingame_mod live-bridge hardening pass: live tracking now clamps corrupted profile values, retries session/profile writes, dedupes ped-based kill/loot tracking more conservatively, guards suspicious reward spikes, and applies conservative live gameplay bonuses through stamina and weapon-wear multipliers so the run produces real in-game effects without adding new pages.
- ingame_mod release-candidate polish: added recent-event dedupe for kill/loot/reward tracking, tightened TrailStage thresholds around real field actions and payout flow, exposed active run bonuses through existing page overrides, and expanded the workspace smoke harness to prove that a corrupt `FrontierLedger.profile.ini` is clamped back into a safe baseline state before detach.
- ingame_mod rc2 hardening pass: loot counting now requires a short stable looting window before incrementing, session/profile saves now flush and emit throttled save/load logs, UI open/close paths force a live-summary refresh, and the default live tracking / live bonus INI values were lowered to safer test-first settings before the latest release candidate build.
- release-candidate packaging pass: added reproducible RC packaging plus backup/install/rollback scripts so the tested `FrontierLedger.asi`, `FrontierLedger.ini`, and Korean LML files can be staged for live-game install without touching the real game folder during preparation.
- release-candidate install-cycle verification: added `ingame_mod\scripts\test_install_cycle.ps1` and proved the RC package can create backups, stage the tested runtime files into a fake game root, and roll that root cleanly back to its original state before any live deployment.
- test-deploy default pass: the native fallback config now boots into `live_ko` without requiring a root INI file and allows `F11` full-ledger testing anywhere, so the real game-root install can stay at a two-file root footprint while still coming up in the intended Korean live-run test mode.
- live RPG rc2 pass: the native live dataset now drives `Stats`, `Skills`, `Perks`, `Crafting`, `Armory`, `Ledger of the Dead`, `Collection`, and `Gear` from real run counters and derived progression layers instead of only showing exported prototype text.
- Korean bitmap polish: widened Hangul glyph spacing, tightened per-line character budgets, added extra runtime string glyph collection, and switched the build to a larger `Malgun Gothic` bitmap set so Korean HUD text has a better chance of rendering cleanly in the in-game path.
- live gameplay effect pass: added a conservative Deadeye delay bonus on top of the existing stamina and weapon-wear bonuses, tied those effects to live bonus tiers / stats / perks, and reset all three modifiers on detach so the runtime exits cleanly.
- save-path hardening follow-up: runtime INI writes now touch/create session/profile files before write/flush, and flush fallback accepts already-written non-empty files so smoke runs no longer report false write failures while still preserving recovery logs and clamps.
- rc2 verification pass: `ingame_mod\\build.ps1`, `python -m unittest discover -s tests`, `python -m frontier_rpg ui --check`, `python .\\scripts\\workspace_smoke_test.py`, `python .\\scripts\\workspace_smoke_test.py --keep-runtime`, and `.\preflight.ps1` all passed from the development folder without touching the live game root.
- Korean HUD readability pass: the native ledger now uses shorter Korean page titles, shorter live-run summaries/details, tighter compact-objective text, safer page entry counts, and wider full-ledger spacing so Hangul data fits the existing Overview/Farming/Network/World and RPG pages without opening a new page or changing the live game folder.
- Korean renderer hardening pass: the bitmap font pipeline now generates `soft_rows + solid_rows` instead of a single hard threshold, the build uses a larger Hangul font sample, and the runtime bitmap string list now includes the Korean HUD footer/title/summary variants that were still missing from the glyph atlas.
- Korean renderer root-fix pass: the native HUD bitmap path now uses a lower-row Hangul font build, pixel-snapped bitmap run drawing, soft-fringe suppression at tiny scales, wider Hangul advance, larger Korean-only layout values, and a non-overlay default INI so Korean text is rendered inside the in-game path instead of relying on the old desktop-overlay fallback.
- zone prompt stabilization pass: the Beecher's Hope `F11` guidance now uses zone-entry hysteresis plus a short grace window before dropping the prompt, logs zone transitions for debugging, and shows a shorter Korean prompt so the in-field 안내문구 no longer jitters at the zone boundary.
- Korean tab-density pass: the full in-game ledger now uses a two-row Korean tab strip with a wider tab header and shifted content panels so more sections are visible at once instead of feeling locked to only a handful of tabs in the full overlay.
- documented the late `2026-03-14` UI audit in the daily work log and development journal, including the Korean UI bottleneck list, implemented-vs-pending native UI checklist, and the recorded `17:19 ~ 17:35 KST` follow-up window for the prompt/tabs/visibility review.
- Korean readability/detail-panel pass: pulled the current live install back out of the real game root, widened the Korean full overlay, enlarged the entry/detail panels, reduced Korean tab density to 4 tabs per row, added clearer list/detail hierarchy labels, strengthened selected-row visibility, and regenerated the bitmap font from a larger Hangul source size so the already-implemented stats/skills/perks detail structure is more visible in the native ledger.
- Korean renderer strategy swap: stopped trying to force the Hangul-heavy page-browser UI through the old bitmap path, added an ASCII-fast native-text path for Korean metric/value lines, and replaced the Korean `F10`/`F11` presentation with a simpler card-style dashboard so the native runtime can show stable Korean headings plus readable ASCII metrics without relying on the previous dense full-ledger text layout.
- Korean F10/prompt polish pass: enlarged the Korean compact dashboard into a clearer three-card field HUD with stronger title/help affordances, and fixed the Beecher's Hope `F11` prompt jitter by treating the zone prompt like an active UI render path so it updates at the fast tick rate instead of the old 50 ms idle cadence.
- late status memo pass: documented that the Korean `F10` / `F11` HUD is now visible enough to move beyond the “UI does not appear” stage, but still needs font / box-size polish and the Korean `F10` payload remains a compact summary rather than the full original design payload.
- late mixed-stack note: documented that the live mixed-mod stack still has unresolved crash risk, the temporary `FrontierLedger-only` isolation experiment was rolled back, and future updates should treat crash analysis and UI polish as separate tracks instead of conflating them.
- workbench interaction vertical-slice pass: added a Beecher's Hope workbench prompt on `E`, a dedicated workbench modal with `Craft / Archive / Result` tabs, and session persistence for the workbench tab plus tab-local entry selections without adding any new ledger pages.
- crafting is now an actual confirm flow inside `ingame_mod`: ready recipes can be committed from the workbench modal, the crafted unlocks persist through `CraftedMask`, and those unlocks immediately feed the existing live progression and run-bonus layer.
- workbench verification passed from the development folder with `ingame_mod\\build.ps1`, `python -m unittest discover -s tests`, `python -m frontier_rpg ui --check`, `python .\\scripts\\workspace_smoke_test.py`, and `.\preflight.ps1`.
- workbench polish pass: reworked the modal into a darker, more workbench-like board layout with a stronger screen scrim, clearer list/detail separation, and a bounded visible-entry window so longer craft lists do not spill off the panel.
- added three new representative craft items to the native workbench flow: `Frontier Tonic Pack`, `Gunsmith Tool Roll`, and `Deadeye Charm`, and expanded the crafted-mask/runtime-bonus layer so those unlocks affect the live run after confirmation.
- expanded the workbench archive with material encyclopedia coverage for salvage, hide bundles, tonic stock, and relic fragments, and tightened the result/detail copy so Korean output is shorter, clearer, and easier to read inside the modal.
- Korean workbench renderer pass: the workbench modal now uses a dedicated Korean render path that avoids long wrapped Hangul paragraphs, draws short Korean headings plus ASCII state/material lines, and uses a stronger opaque board layout so the body content is readable instead of only the header tabs.
- Korean workbench visibility follow-up: the modal now uses short ASCII row labels, larger Korean/ASCII body text, stronger selected-row/status contrast, and a simpler `LIST / DETAIL / RUN` structure so more than the first entry is actually readable in live footage.
- Korean workbench label-polish pass: the latest workbench build replaces the most confusing mixed labels with short Korean list names, enlarges the small body/footer text another step, and routes craft/archive/result summaries through cleaner Korean detail helpers before live installation.
- 2026-03-15 workbench follow-up: pushed the native workbench deeper with more representative craft items, broader material-archive coverage, repeated Korean renderer/layout passes, and live install verification; the remaining blocker is now final Korean workbench presentation cleanup rather than missing interaction logic.
- 2026-03-15 refactor audit pass: audited the full `E:\RDR2 RPG MODS` workspace, confirmed the main code-risk cluster is still the native workbench/Korean UI path in `ingame_mod\\src\\frontier_ledger.c`, neutralized an extra duplicate Korean render branch, and documented the remaining high-risk duplication points that should be collapsed before further feature work.
- added a dedicated workbench checklist document that tracks the three native workbench categories (`Craft`, `Archive`, `Result`) against in-game implementation criteria and verification gates, so future work can distinguish between functional completion and presentation polish.
- story/family push pass: added native Surveyor clue tracking, sky-devil myth state, Abigail/Jack/Uncle support ranks, ranch threat/defense state, farming-lane recommendations, and page/detail/bonus integration so the `00` story draft and `04` family/ending/farming draft now have real in-game runtime hooks instead of only document-level intent.
- workbench Korean cleanup pass: collapsed the active workbench overlay and confirm flow down to one locale-aware path in `ingame_mod\\src\\frontier_ledger.c`, removed dead helper/render branches, widened the Korean workbench layout, and routed list/detail/result rendering through the same authoritative helpers instead of leaving stale side paths behind.
- workbench glyph coverage follow-up: expanded `ingame_mod\\runtime_bitmap_strings.txt` with the active Korean workbench labels/messages so the rebuilt bitmap atlas includes the characters actually used by the current in-game workbench UI.
- verification rerun after the cleanup pass succeeded with `ingame_mod\\build.ps1`, `python -m unittest discover -s tests`, `python -m frontier_rpg ui --check`, `python .\\scripts\\workspace_smoke_test.py`, and `.\preflight.ps1` from the development workspace.

## 2026-03-16

- English-first baseline pass: changed the active native config and tests to default to `live_en` with `PreferredLocale=en`, so the workspace stops drifting back into Korean-first behavior during session restore while render/layout rules are being stabilized.
- locale seam pass: added `ingame_mod\\locales\\README.md`, `ingame_mod\\locales\\ko\\README.md`, and `ingame_mod\\locales\\ko\\FrontierLedger.ko.example.ini` so Korean remains a parked option path instead of blocking the active English rebuild.
- workbench text pass: shortened the active English workbench helper/detail/result strings into shorter ASCII-safe lines and reused the same compact `materials/effect/gate` format across craft/result flows instead of mixing long prose with short status text.
- render-path cleanup pass: removed the now-unused native/Korean-only short-text helper functions from the active path and collapsed the workbench overlay onto the same `frontier_draw_text_short` / `frontier_draw_wrapped_text` rule set used by the rest of the in-game UI.
- runtime logging pass: updated native attach/session logs to report the normalized active dataset plus locale preference, making future English/Korean verification easier to audit.
- video-guided overlap follow-up: reviewed the real in-game `20260316_191035.mp4` capture plus the live log, identified the remaining overlap as a compact-footer and full-detail-panel spacing issue rather than a workbench-wide renderer issue, then tightened the English panel line budgets and compact footer spacing before reinstalling the refreshed build.
- live growth interaction pass: the full native ledger now accepts `Confirm/E` on the `Stats` and `Skills` pages, persists manual growth bonuses in `FrontierLedger.profile.ini`, and shows action-oriented live labels with remaining point counts.
- crafting and armory bridge pass: the full ledger can now trigger the existing craft flow directly from the `Crafting` page and route `Armory` selections into weapon-focused crafting when a relevant upgrade is ready, instead of leaving those pages fully read-only.

## 2026-03-17

- memory-foundation pass: added a `Memory` folder with troubleshooting, navigation index, domain knowledge, and architecture notes so repeated failures and large-file search patterns do not need to be rediscovered every turn.
- request-scope skill pass: added `frontier-ledger-current-request-focus` and registered it in `AGENTS.md` so future turns can stay anchored to the current ask instead of drifting into stale backlog context.
- affinity-doc pass: added `기획문서\\09_affinity_relationships_draft.md` and linked the new affinity axis back into the consolidated design and implementation blueprint docs.
- prototype-structure pass: started the actual split with new `frontier_rpg\\core`, `frontier_rpg\\data`, `frontier_rpg\\features`, `frontier_rpg\\game`, and `frontier_rpg\\platform` packages instead of leaving all new work inside the old monolith.
- affinity-feature pass: added a standalone prototype affinity module plus authored affinity data for Abigail, Jack, and Uncle, including stage thresholds, world-tier gates, repeatable reward scaling, CLI inspection, and regression coverage.
- affinity-ui pass: exposed affinity through the prototype full UI as a dedicated tab instead of leaving it accessible only through CLI and family-detail text.
- affinity-network pass: connected affinity ownership to repeatable reward scaling summaries inside the prototype network/integration output so relationship state now affects another category beyond family rows.
- story-affinity doc pass: updated the family and first-10-hours design docs so affinity is treated as part of the opening hub loop and family progression, not a detached late add-on.
- live-reference preservation pass: pulled the exact currently installed Frontier Ledger package back into `ingame_mod\\live_reference\\2026-03-16_live_verified_current`, including the live ASI, INI, LML text pack, log, session/profile files, and removal manifest so the workspace now keeps the readable live baseline in one place.
- live/dist verification pass: confirmed that the current live `FrontierLedger.asi` and `strings.gxt2` match the workspace `dist` outputs by SHA256, so the “good live build” is the same package already preserved under development outputs.
- current-state doc sync pass: updated `기획문서\\07_master_design_consolidated.md` and `기획문서\\08_large_scale_implementation_blueprint.md` so they no longer incorrectly describe the project as concept-only.
## 2026-03-17 Late Follow-Up

- affinity midgame-loop pass: replaced the old score-only placeholder with authored activity gains, stage 1~2 affinity quest clears, reward package grants, affinity-gated perks, homefront request surfacing, and a native export-facing `Affinity` page.
- category-refactor pass: split `Homefront`, `Farming`, `Network`, `World`, and `Ending` row/state generation out of `frontier_rpg\\systems.py` into dedicated feature modules so those systems now own their own logic instead of staying in one aggregation file.
- category-package pass: added milestone / package claim logic to `Farming`, `Network`, `World`, and `Ending`, so those categories now generate real inventory, material, and collection results during simulation and endgame projection instead of only rendering static summaries.
- live clean-install pass: moved the previously installed `Frontier Ledger` files into `E:\SteamLibrary\steamapps\common\Red Dead Redemption 2\codex_backups\FrontierLedger_clean_install_20260317_210545`, rebuilt the current workspace, and reinstalled the live ASI / INI / LML package until all hashes matched the current `dist` outputs.
- live log verification pass: confirmed the post-install game run loaded `FrontierLedger.asi` successfully under ScriptHookRDR2, produced no obvious `error` / `failed` / `exception` lines in the inspected tail, advanced the live run to `Phase 1 / TrailStage 1`, counted one hub return, and persisted one `Gunsmithing` skill bonus.

## 2026-03-18

- combat materialization pass: added encounter-owned combat definitions for elites and bosses, including HP, armor, pressure, resistances, weaknesses, boss phases, support pressure, and test-spawn plans, and wired combat resolution into XP, drops, individual boss defeat state, and collection progress.
- live gameplay-effect follow-up: strengthened the native runtime bonus layer so stats, perks, family support, and crafted unlocks now influence the applied stamina, weapon-wear, and deadeye modifiers more directly during live datasets.
- world-route loop pass: added route-node definitions and prototype resolution flow so the workspace now supports `kill -> drop -> pickup -> stash` behavior instead of only abstract material gain.
- stash-aware crafting pass: crafting can now consume both field and Beecher's Hope storage materials, and effective recipe cost is adjusted by skills and perks rather than staying fully static.
- growth-effect centralization pass: added a dedicated growth-effect seam so combat damage, boss damage, resist, loot yield, drop quality, travel stamina, mounted control, medicine efficiency, and craft-cost reduction all have explicit derived outputs.
- prototype accessibility seam: added an English / Korean shell-level selector to the desktop UI and split shell labels into a dedicated localization helper instead of leaving them hard-coded in the UI.
- export/localization sync pass: widened the native export translation tables and regenerated the native headers so the new route/storage wording is represented in the workspace outputs and Korean dataset path.
- verification for the workspace-only finish pass completed with `python -m unittest discover -s tests`, `python -m frontier_rpg combat --simulated-profile`, `python -m frontier_rpg loot --node node.beechers_supply_drop --simulate`, `python -m frontier_rpg effects --simulated-profile`, `python -m frontier_rpg ui --check`, `ingame_mod\\build.ps1`, `python .\\scripts\\workspace_smoke_test.py`, and `preflight.ps1`, all without touching the live game folder.

## 2026-03-19

- native spawn rollout pass: added a real native spawn hook path for regional encounter summons and boss-archive summons, then hardened it by moving the actual spawn execution out of the keyboard callback and into the runtime tick as a pending request.
- route/storage expansion pass: widened world route node coverage across more regions and storage destinations so the pickup/storage loop is less placeholder-like and more region-owned.
- live gameplay hook expansion pass: extended the active live modifier stack beyond stamina / weapon wear / deadeye to include health recovery, weapon damage, melee damage, horseback mitigation, and movement-rate support.
- Korean body-text follow-up: added more Korean coverage for the newly added combat/world-route/growth-heavy detail text in the prototype seam and export translation path.
- first balance pass: reduced some common route drop volume, tightened boss material output, and tuned effective recipe discount strength so progression is less inflated than the previous pass.
- clean live install pass: installed the latest 2026-03-19 build into the real RDR2 root, confirmed live ASI / INI / LML hashes match the current workspace `dist` outputs, and reset the live Frontier Ledger runtime files for cleaner verification.
- live validation result: confirmed the latest build is the one actually loading in-game, confirmed the spawn trigger now creates encounter peds successfully, and confirmed route / region progression reacts live; however, the tested run still did not reflect `EnemyKills`, `AnimalKills`, `LootCount`, or `RewardEarned` in the saved live profile after the user's reported combat session, so live action counter reflection remains the next fix target.

## 2026-03-20

- live counter repair pass: widened kill attribution, improved spawned-ped tracking behavior, and stopped relying only on the original source-of-death check so live field results can finally reflect into the saved profile.
- live spawn visibility pass: pushed the regional spawn path further away from the earlier transparent/player-lookalike behavior and confirmed the user can now actually see the spawned enemies in-game.
- live runtime confirmation pass: verified by log/profile evidence that `EnemyKills`, `AnimalKills`, `LootCount`, and `RewardEarned` now reflect actual field actions in the tested live run instead of staying stuck at zero.
- live progression confirmation: the same run also showed support-rank and trail-stage progression continuing to react on top of the repaired counter path, which makes the native encounter loop much more trustworthy for future balance work.
- next blocker narrowed: the project can now move past the regional visible-spawn and basic live-counter barrier; the next live-facing step should be dedicated boss-archive summon verification plus follow-up tuning and cleanup.

## 2026-03-21

- buildout uplift pass: widened the weaker planning-doc categories with more authored first-10-hours content, more chapter granularity, stricter story/world-tier gates, blueprint-aware crafting lines, named-weapon/ammo routes, and more world-node coverage across prototype, UI, export, and tests.
- two-round validation pass: revalidated the widened content through targeted CLI checks plus broad workspace checks, including `python -m unittest discover -s tests`, `python -m frontier_rpg ui --check`, `.\build.ps1`, `python .\scripts\workspace_smoke_test.py`, and `.\preflight.ps1`.
- live install refresh pass: backed up the previous game-root install again, redeployed the current `dist` package into the real RDR2 folder, and confirmed the live ASI/INI/LML files hash-match the workspace outputs.
- native authored-spawn diagnosis pass: confirmed from the user's screenshot plus live log/profile/session files that generated/exported `Rusk`, `Dead Courier`, blueprint, and world-route content already existed, but the live runtime was still selecting only the older generic regional spawn profiles.
- authored spawn router fix: updated `ingame_mod\src\frontier_ledger.c` so field spawns can choose more contextual/authored profiles by progression and so full-ledger selected entries can queue a linked encounter path instead of relying only on the boss archive.
- live proof of the router fix: the newest live run loaded `Build=2026-03-21-authored-spawn-pass` and the log now shows `Rusk, The Collector's Dog` spawning in the field instead of only the older `Blackwater Mercenary Patrol` route.
- remaining live-facing gap: authored encounter spawning is now materially better, but corpse/prompt presentation can still look generic after death and the native workbench still does not fully mirror the widened exported blueprint/crafting list.

## 2026-03-22

- authored sync pass: upgraded the native build to `2026-03-22-authored-sync-pass`, moved selected-entry authored encounter routing onto the normal full-ledger confirm path, and stabilized boss archive lookup by matching the selected boss entry text instead of assuming page index alone.
- workbench parity pass: aligned the native live craft mask and workbench view with the exported 13-recipe crafting route set, switched the workbench craft list/detail flow toward export-driven labels/details, and added regression checks for the widened crafting and dead-page counts.
- corpse identity pass: added authored-corpse prompt surfacing for Frontier-spawned dead enemies so the player keeps seeing authored encounter identity even after the fight is over.
- reward pacing pass: stopped the prototype from repeatedly re-crafting unique upgrades during the authored first-10-hours simulation, which removes the duplicated `Saint Medal` / `Blackwater Precision Loads` item inflation and gives a cleaner authored reward curve.
- workspace + live package verification: re-ran `python -m unittest discover -s tests`, `python -m frontier_rpg ui --check`, authored simulate/combat/loot probes, `.\build.ps1`, `python .\scripts\workspace_smoke_test.py`, `.\preflight.ps1`, and reinstalled the current package into the real game folder with post-install ASI / `strings.gxt2` hash matches.
- remaining live-facing gap: today's code closes much more of the workspace/native parity gap, but there is still no fresh in-game log on the new build proving that `confirm-selected-queue`, `confirm-boss-queue`, and the corpse prompt card all fired during a real gameplay run.
