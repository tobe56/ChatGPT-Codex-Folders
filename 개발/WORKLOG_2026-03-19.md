# 2026-03-19 Worklog

## Summary

Today was the first post-3/18 rollout day that moved from workspace-only conversion into real in-game bridge work.
The main goal was to attach the native spawn path, widen the route/storage and live-effect layers, install the new build into the actual RDR2 folder, and then learn from live behavior instead of guessing.

## What Landed

- added a real native spawn debug path for regional encounters and boss-archive summons
- moved the spawn action out of the original direct input path and into a safer pending-request flow processed by the main runtime tick
- expanded world route nodes so more regions now have authored pickup/storage destinations
- widened live gameplay hooks beyond stamina, weapon wear, and deadeye into health regen, health regen timing, weapon damage, melee damage, horseback damage reduction, and move-rate override
- added more Korean coverage for the new combat / route / growth-heavy body text and export strings
- applied a first-pass balance pass to common route drops, boss material volume, and effective crafting discount strength
- installed the new build into the real Red Dead Redemption 2 folder and confirmed the live ASI / INI / LML hashes match the current `dist` outputs

## Live Test Outcome

The most important live findings today were mixed:

- the latest build is definitely loading live
- the new spawn path now really fires in-game
- regional encounter spawning now succeeds instead of hard-crashing immediately
- route / region progression hooks are visibly updating

But one important system still did not reflect correctly in the tested live run:

- `EnemyKills`
- `AnimalKills`
- `LootCount`
- `RewardEarned`

remained at zero in the saved live profile after the user's reported combat and hunting actions.

So the correct end-of-day read is:

- `spawn path`: now live and functioning
- `kill / loot tracking reflection after spawned fights`: still not solved

## What Changed In The Live Runtime

### Input / Spawn

- the debug spawn key moved off the bad `F6` choice and now uses `F8`
- the runtime no longer tries to do the risky spawn work directly inside the keyboard callback
- instead it queues a pending spawn request and processes it during the main runtime tick

### Route / Storage

- route coverage is broader than yesterday
- stash destinations are no longer all the same placeholder destination
- pickup/storage flow is clearer in both prototype output and exported dataset text

### Live Effects

- the native runtime now computes and applies more than the older stamina / wear / deadeye trio
- health recovery and weapon/melee damage layers are now part of the live modifier stack

## Validation

Validated today with:

- `python -m unittest discover -s tests`
- `python -m frontier_rpg loot`
- `python -m frontier_rpg loot --node node.new_hanover_ore_camp`
- `python -m frontier_rpg loot --node node.beechers_supply_drop --simulate`
- `python -m frontier_rpg combat --simulated-profile`
- `python -m frontier_rpg effects --simulated-profile`
- `python -m frontier_rpg ui --check`
- `.\build.ps1`
- `python .\scripts\workspace_smoke_test.py`
- `.\preflight.ps1`
- live install hash comparison for ASI / INI / install.xml / strings.gxt2
- live `FrontierLedger.log` inspection after real in-game testing

## Practical End-Of-Day State

The project is now past the point where the native spawn path is hypothetical.
It exists, the live build loads it, and the user has already exercised it in the actual game runtime.

The current blocker is narrower now:

- the live combat/hunt action counters still need to reflect correctly after those spawned encounters and normal field kills

That is a much better problem than the earlier immediate-crash state.

## Tomorrow Focus

Tomorrow should continue from the current live-tested state, not from the older workspace-only assumptions.

Priority order:

1. fix live `EnemyKills` / `AnimalKills` / `LootCount` / `RewardEarned` reflection after real field actions
2. verify whether the spawned encounter peds are bypassing the current kill-source detection path
3. confirm boss-archive summon flow separately from regional field spawn
4. tighten the live key choice and any remaining key conflicts if needed
5. only after the live counters are stable, continue the next balance and world-placement pass
