# 2026-03-22 Worklog

## Summary

Today focused on turning yesterday's "known gaps" into concrete code paths instead of leaving them as follow-up notes:

- full-ledger selected entries can now queue their linked authored encounter from the normal confirm flow, not only through the older spawn-debug seam
- boss-archive selections now route through a more stable boss-profile lookup instead of depending only on raw page index assumptions
- native workbench craft labels/details are now tied much more closely to the exported crafting dataset, including the widened 13-recipe list
- authored corpse identity now leaves a native prompt-level trace so the result feels less like an anonymous vanilla body after death
- the prototype reward/crafting flow no longer re-crafts unique upgrades repeatedly during the authored first-10-hours script, which was inflating item/reward pacing

## What Changed

- upgraded the native build tag to `2026-03-22-authored-sync-pass`
- expanded the native craft mask from the old 11 synthetic slots to a 13-entry mapping aligned to the exported crafting route order
- moved full-ledger authored encounter routing onto the normal `Confirm / E` path for encounter-linked entries, with added context logging
- hardened boss archive routing by matching selected boss entries by text content instead of trusting only page position
- synced live dead-page labels more closely to the five exported boss rows, including `Tall Trees Tracker King` and `The Surveyor`
- replaced the native workbench craft list/detail dependency on the old hardcoded mini-list with export-driven label/detail text for the live dataset
- added a corpse-adjacent authored prompt card for Frontier-spawned dead enemies so the player still sees authored identity near the body
- prevented the prototype from re-crafting unique items like `Saint Medal` and `Blackwater Precision Loads` every time the automated first-10-hours path rechecked recipes
- reinstalled the rebuilt package into the real RDR2 game folder and confirmed the live ASI plus `strings.gxt2` hash-match the workspace `dist` outputs

## Late Regression Investigation

The later half of the day was dominated by one problem:
the spawn path stopped behaving like the earlier visible live builds and started oscillating between partial bodies, invisible-looking enemies, and outright `F8` crashes.

The important progress from that deeper pass:

- extracted build tags from the archived live ASI backups instead of guessing which day had the last trustworthy visible spawn
- confirmed the visible live lineage was the older `2026-03-19-spawn-route-pass` family, not the newer authored-routing passes
- compared the current native spawn function against the archived `frontier_ledger.c` inside `개발 (25).zip` and `개발 (26).zip`
- confirmed multiple regression layers were introduced after the visible build:
  - selected-entry routing widened
  - donor-ped clone fallback came back into the live path
  - `REQUEST_MODEL` / `CREATE_PED` flag combinations drifted
  - extra corpse-prompt / debug complexity was added while the spawn path was still unstable
- walked those regressions back in stages and revalidated each stage through build, tests, smoke, and preflight instead of continuing to pile new ideas on top

The latest shipped live ASI at end of day is:

- `2026-03-22-donor-disable-pass`

That package is hash-matched between workspace `dist` and the actual game-root `FrontierLedger.asi`.

## Validation

- `python -m unittest discover -s tests`
- `python -m frontier_rpg ui --check`
- `python -m frontier_rpg simulate`
- `python -m frontier_rpg combat --encounter encounter.rusk_collectors_dog --simulated-profile`
- `python -m frontier_rpg loot --node node.blackwater_dead_courier --simulate --simulated-profile`
- `.\build.ps1`
- `python .\scripts\workspace_smoke_test.py`
- `.\preflight.ps1`
- `.\install_to_game.ps1 -GameRoot E:\SteamLibrary\steamapps\common\Red Dead Redemption 2`
- post-install hash comparison for live `FrontierLedger.asi`
- post-install hash comparison for live `strings.gxt2`

## Remaining Gap

The workspace and live install are now on the new late-day stability-oriented package, but the final confidence gap is still true in-game confirmation:

- the remaining blocker is no longer "is there a spawn path at all" but "which exact low-level live spawn configuration stops bodyless metapeds and `F8` crashes under the user's real mod stack"
- selected-entry and boss-archive authored routing code exists, but the live spawn lane itself is not trustworthy enough yet to call those checks done
- category coverage is still closer to the 90%+ built target than yesterday, but the unclosed live spawn regression means the final polish bar is still gated by runtime evidence

## Tomorrow Focus

Only these two tasks should matter tomorrow:

1. isolate and fix the remaining live `F8` spawn regression until enemies render as full bodies without crashes in the real game process
2. once visible live spawn is trustworthy again, re-run only the two blocked live confirmations: full-ledger selected-entry summon and boss-archive summon
