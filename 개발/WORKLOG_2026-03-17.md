# 2026-03-17 Worklog

## Summary

Today started with structure, not speed-at-all-costs feature sprawl.
The goal is to make the next large implementation passes faster by reducing rediscovery cost, giving the workspace a stable memory layer, and starting the affinity system through a proper feature split instead of one more monolith patch.

## What Changed

- created a `Memory` folder for reusable agent-facing docs
- documented recurring failure modes, workspace search index, domain rules, and refactor targets
- added a new current-request-focus skill so future turns can stay anchored to the user's current ask
- created `기획문서/09_affinity_relationships_draft.md`
- started the Python-side structure split with `core`, `data`, `features`, `game`, and `platform`
- added a standalone prototype affinity module and authored affinity data for Abigail, Jack, and Uncle
- exposed affinity through the prototype CLI and family/homefront output

## Why This Order

The project is now large enough that careless exploration time is becoming expensive.
Without a memory layer and a clearer split target, every new feature risks becoming slower to land and harder to verify.

The first step today was therefore:

1. preserve knowledge
2. reduce future search cost
3. add the next feature through the intended architecture instead of the old one

## Affinity Scope Landed Today

The affinity prototype now includes:

- main NPC coverage for Abigail, Jack, and Uncle
- stage thresholds from 1 to 5
- world-tier gates on stages 4 and 5
- reward-package definitions per stage
- repeatable reward bonus scaling
- gift and dialogue delta helpers
- CLI inspection through `python -m frontier_rpg affinity`

What is not done yet:

- native in-game affinity interaction
- dedicated affinity quest execution logic
- perk / item / weapon payout binding from cleared affinity quests
- repeatable NPC reward scaling inside the native live runtime

## Immediate Next Steps

1. add perk spending to the live native ledger
2. improve armory feedback and item/resource spend loops in the live runtime
3. connect prototype affinity rewards to concrete perk, gear, and quest outputs
4. begin the first authored Blackwater -> Silas Redd vertical slice

## Notes For Tomorrow

- read `Memory/API_Index.md` first
- read `Memory/TroubleShootings.md` before any `frontier_ledger.c` heavy edit
- keep affinity as a standalone feature module
- do not turn the next pass into another page-only UI expansion

## Follow-Up Progress In The Same Day

I continued past the foundation pass and pushed the four requested tracks forward inside the workspace.

What moved:

- `Interaction UI`: the prototype UI now exposes `Affinity` as a first-class full-ledger tab instead of leaving it as CLI-only state
- `Story / setting`: the family and first-10-hours docs now explicitly pull affinity into the early hub loop and family progression tone
- `Affinity`: the prototype now ties affinity into repeatable reward scaling so it affects more than a standalone row list
- `Refactor direction`: the new feature/data/core split is now being used by real output paths instead of existing only as empty folders

Concrete effects after the follow-up pass:

- `python -m frontier_rpg affinity` works
- the full prototype UI can render an `Affinity` tab
- `build_integration_rows()` now exposes repeatable reward bonus context through affinity ownership
- the simulation snapshot now shows affinity stages beside homefront state

This still does not mean the entire 3/17 roadmap is finished.
It means all four requested tracks are now actively started in code and docs, with real output surfaces using them.

## Late-Day Consolidation

I kept going after the earlier affinity and refactor pass and closed the day around a more defensible implementation state.

What changed after the earlier notes:

- `Affinity` moved beyond score rows into activity-driven gains, stage 1~2 quest clears, reward package grants, affinity-linked perks, and a native export-facing page.
- `Homefront`, `Farming`, `Network`, `World`, and `Ending` were all split into feature-owned modules instead of staying embedded inside `frontier_rpg/systems.py`.
- `Farming`, `Network`, `World`, and `Ending` now include real milestone or package loops that grant materials, items, and collection state during simulation or endgame projection instead of only rendering summary rows.
- the native ledger export is now effectively a `19-page` shape because `Affinity` is part of the full page set.

## Live Install Finish

The day also ended with a clean live install pass in the real game folder.

Key facts:

- the previous `Frontier Ledger` files were rotated into `E:\SteamLibrary\steamapps\common\Red Dead Redemption 2\codex_backups\FrontierLedger_clean_install_20260317_210545`
- the current live `FrontierLedger.asi`, `FrontierLedger.ini`, `install.xml`, and `strings.gxt2` now hash-match the current workspace `dist` outputs
- the user reported the in-game UI looked correct after the reinstall
- the inspected live log showed normal attach, no obvious error/failure lines, one stored Gunsmithing skill bonus, one hub return, and live phase/trail advancement

One caveat still matters:

- the live log continues to print the stale build-tag string `2026-03-16-live-growth-pass`, so build identity should be judged by file hashes first, not by that one label

## Tomorrow Plan

Tomorrow should pivot away from more page growth and into world-behavior conversion.

Priority order:

1. enemy AI and boss-kill resolution hooks
2. world item placement, material drop flow, and stash / storage routing
3. live perk / stat / skill effects that apply directly to gameplay instead of only ledger summaries
4. balancing passes for item value, drop amount, recipe costs, and boss reward strength
