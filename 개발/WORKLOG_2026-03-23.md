# 2026-03-23 Worklog

## Summary

Today focused on optimization and structural cleanup rather than adding new gameplay features:

- created a full pre-work snapshot for the `개발` workspace and locked the baseline with `PLAN.md`, `WORKLOG.md`, and an active-code validation policy
- reduced the main Python bottlenecks by turning `content.py`, `systems.py`, `ui.py`, and `ui_state.py` into facades over smaller `data`, `game`, and `ui_pages` modules
- optimized native maintainability by splitting `frontier_ledger.c` into staged include files for helper, render, persistence, input/overlay, and workbench/spawn responsibilities
- added workspace-only `ruff` and scoped `mypy` checks so the refactor can keep moving without dragging generated or artifact folders into the quality gate
- finished two consecutive full validation rounds across lint, scoped typecheck, unit tests, UI check, CLI probes, build, smoke, and preflight

## What Changed

- created `_snapshots/20260323_2124` with `workspace_snapshot.zip` and `manifest.json`
- added `PLAN.md`, `WORKLOG.md`, `FINAL_REFACTOR_REPORT.md`, `pyproject.toml`, and `requirements-dev.txt`
- moved authored content definitions out of `frontier_rpg/content.py` into:
  - `frontier_rpg/data/world_content.py`
  - `frontier_rpg/data/progression_content.py`
  - `frontier_rpg/data/social_content.py`
  - `frontier_rpg/data/combat_content.py`
- moved prototype orchestration out of `frontier_rpg/systems.py` into:
  - `frontier_rpg/game/state.py`
  - `frontier_rpg/game/rows.py`
  - `frontier_rpg/game/progression.py`
  - `frontier_rpg/game/simulation.py`
- moved UI shell/state builders out of the old monoliths into:
  - `frontier_rpg/ui_app.py`
  - `frontier_rpg/ui_widgets.py`
  - `frontier_rpg/ui_pages/*`
- split `ingame_mod/src/frontier_ledger.c` into staged include files:
  - `frontier_ledger_helpers_core.inl`
  - `frontier_ledger_helpers_text.inl`
  - `frontier_ledger_live_profile.inl`
  - `frontier_ledger_persistence.inl`
  - `frontier_ledger_render_overlay.inl`
  - `frontier_ledger_render_compact.inl`
  - `frontier_ledger_render_full.inl`
  - `frontier_ledger_overlay_control.inl`
  - `frontier_ledger_input_loop.inl`
  - `frontier_ledger_spawn.inl`
  - `frontier_ledger_workbench.inl`

## Optimization Outcome

- `frontier_rpg/content.py`: `2176 -> 202` lines
- `frontier_rpg/systems.py`: `1229 -> 81` lines
- `frontier_rpg/ui.py`: `645 -> 4` lines
- `frontier_rpg/ui_state.py`: `574 -> 49` lines
- `ingame_mod/src/frontier_ledger.c`: `11705 -> 1550` lines

The main gain today was not new content.
It was lowering navigation cost, shrinking blast radius, and making the codebase cheaper to search, edit, and revalidate.

## Validation

- `python -m ruff check frontier_rpg tests ingame_mod/scripts`
- `python -m mypy --follow-imports skip frontier_rpg/game frontier_rpg/core frontier_rpg/ui_pages frontier_rpg/ui_app.py frontier_rpg/ui_widgets.py frontier_rpg/ui_state.py frontier_rpg/systems.py`
- `python -m unittest discover -s tests`
- `python -m frontier_rpg ui --check`
- `python -m frontier_rpg simulate`
- `python -m frontier_rpg affinity`
- `python -m frontier_rpg combat --simulated-profile`
- `python -m frontier_rpg loot --simulate --node node.blackwater_dead_courier --simulated-profile`
- `python -m frontier_rpg effects --simulated-profile`
- `.\build.ps1`
- `python .\scripts\workspace_smoke_test.py`
- `.\preflight.ps1`
- repeated the same full gate a second time and passed again

## Remaining Gap

- this was a workspace-only optimization/refactor day, not a live visual verification day
- scoped `mypy` now protects the new structure, but deeper typing of legacy feature modules can still be expanded later
- the native file is structurally split, but there is still room to push the include-based split into fuller translation-unit separation in a later pass

## Tomorrow Focus

Tomorrow I will continue from this optimized baseline instead of starting a new feature branch of work:

1. keep pushing the post-refactor cleanup where it has the highest payoff, especially around follow-up typing scope and any remaining facade friction
2. continue the next optimization/stabilization pass on top of the now-split structure rather than reopening the old monolith-first workflow
