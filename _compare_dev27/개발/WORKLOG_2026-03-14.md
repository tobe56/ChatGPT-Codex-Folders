# Work Log - 2026-03-14

## Today Summary

Today's work stayed centered on `ingame_mod` and focused on raising real mod completeness instead of expanding the prototype layer.

## F10 Visibility And Zone-Prompt Jitter Pass

- Re-checked the latest live capture against the runtime log and confirmed that `F10` was opening, but the compact Korean dashboard was still too small and easy to miss during motion.
- Confirmed the remaining Beecher's Hope prompt flicker was mostly a render-cadence issue, not a fresh zone-boundary bug. The prompt was being drawn while the script loop had already dropped back to the slow `Wait(50)` idle path.
- Enlarged the Korean `F10` compact dashboard with a wider panel, stronger title/help affordance, larger combat/hunting cards, and a clearer progress block.
- Promoted the idle zone prompt into the fast-tick path so the `F11` prompt now updates like an active HUD element while the player is standing inside the allowed zone.
- Strengthened the prompt strip itself with a larger background and accent header to improve stability and readability at the bottom of the screen.

### Verification After F10/Prompt Pass

- `.\build.ps1`
- `python -m unittest discover -s tests`
- `python .\scripts\workspace_smoke_test.py`
- `.\preflight.ps1`

All passed from the development folder.

## Late Status Wrap-Up (`2026-03-14 19:42 ~ 20:12 KST`)

- Rechecked the latest in-game captures and confirmed that the native Korean HUD has crossed the “does not show up” threshold:
  - `F10` compact HUD is visible in live footage
  - `F11` full HUD is visible in live footage
  - the current blocker is now polish/readability, not total UI failure
- Recorded the honest UI state:
  - `F10` is now a working compact dashboard, but it still carries a reduced payload compared with the full original design
  - `F11` is usable as a Korean dashboard, but it is still a fallback presentation rather than the final Korean version of the original tab/list/detail concept
  - stat/skill/perk/crafting detail structures are implemented underneath, but Korean presentation still prioritizes stable visibility over full interaction density
- Captured the immediate follow-up memo for later updates:
  - enlarge / clean the Korean font and box sizing one more step
  - enrich the `F10` payload with a little more of the planned field summary
  - return to interaction work only after the Korean presentation layer is stable enough
- Logged the mixed-stack caution:
  - a temporary `FrontierLedger-only` isolation attempt caused a separate load-stage crash and was fully rolled back
  - mixed-mod crash analysis remains unresolved and should be treated separately from UI polish

The main goal was to harden the first live gameplay bridge, make the tracked counters more trustworthy, and connect that live run state back into actual gameplay effects.

## What Was Finished Today

### 1. Live Counter Bridge Hardening

- Hardened the native live counters for:
  - enemy kills
  - loot count
  - animal kills
  - reward earned
- Added more conservative entity-based dedupe behavior for tracked nearby peds.
- Reset tracked ped state when a handle appears alive again so reused handles do not stay permanently "counted".
- Added reward spike guards so very large one-sample cash jumps are ignored instead of polluting `RewardEarned`.
- Kept negative cash changes from subtracting out of the live reward total.

Key file:

- `E:\RDR2 RPG MODS\개발\ingame_mod\src\frontier_ledger.c`

### 2. Profile / Session Safety Hardening

- Added safer session/profile save behavior with retry-based INI writes.
- Added clamp-based recovery when loading damaged or out-of-range values from `FrontierLedger.profile.ini`.
- Kept the existing live/profile/session structure intact while making it less fragile.

Key files:

- `E:\RDR2 RPG MODS\개발\ingame_mod\src\frontier_ledger.c`
- `E:\RDR2 RPG MODS\개발\ingame_mod\src\ledger_config.h`
- `E:\RDR2 RPG MODS\개발\ingame_mod\src\ledger_config.c`

### 3. Real Gameplay Effects Added

- Added a conservative live stamina bonus:
  - higher stamina recharge
  - lower sprint stamina depletion
- Added a conservative weapon wear bonus:
  - reduced weapon degradation modifier
- These bonuses are tied to live run bonus tiers driven by `TrailStage`, activity counters, reward flow, and hub returns.

Key file:

- `E:\RDR2 RPG MODS\개발\ingame_mod\src\frontier_ledger.c`

### 4. Existing Page Reinforcement

- Strengthened existing page overrides only. No new pages were added.
- `Overview` now exposes the current run bonus and real activity summary.
- `Farming` now shows hunt/loot/field activity with enemy and animal pressure detail.
- `Network` now shows reward intake, hub-return progress, and loot-driven conversion.
- `World` now shows live pressure plus current run bonus state.

Key file:

- `E:\RDR2 RPG MODS\개발\ingame_mod\src\frontier_ledger.c`

### 5. Config Surface For The New Logic

- Added live tracking and bonus settings to the existing config path:
  - poll interval
  - tracked ped TTL
  - reward delta clamp
  - stamina bonus enable
  - weapon wear bonus enable
  - per-tier bonus scales

Key files:

- `E:\RDR2 RPG MODS\개발\ingame_mod\src\ledger_config.h`
- `E:\RDR2 RPG MODS\개발\ingame_mod\src\ledger_config.c`
- `E:\RDR2 RPG MODS\개발\ingame_mod\FrontierLedger.ini`

## Verification Run Today

- `.\build.ps1`
- `python -m unittest discover -s tests`
- `python -m frontier_rpg ui --check`
- `python .\scripts\workspace_smoke_test.py`
- `.\preflight.ps1`

All of the above passed in the workspace without copying a new build into the live game folder.

An additional RC install-cycle check now also passes through `.\scripts\test_install_cycle.ps1`, which verifies backup creation, staged install, and rollback on a fake game root.

## Build Candidate

- `E:\RDR2 RPG MODS\개발\ingame_mod\build\FrontierLedger.asi`
- `E:\RDR2 RPG MODS\개발\ingame_mod\dist\FrontierLedger\FrontierLedger.asi`
- `E:\RDR2 RPG MODS\개발\ingame_mod\dist\FrontierLedger\FrontierLedger.ini`

## What Improved Today

- live counters are more trustworthy than the previous pass
- existing pages now reflect live activity more concretely
- the run now produces real gameplay benefit instead of only tracking state
- the workspace build is now closer to a serious install candidate
- the release-candidate package now includes backup/install/rollback tooling that was verified before any real game-folder deployment

## Release Candidate Package

- `E:\RDR2 RPG MODS\개발\ingame_mod\release_candidate\FrontierLedger_RC1`
- `E:\RDR2 RPG MODS\개발\ingame_mod\release_candidate\FrontierLedger_RC1.zip`

## Remaining Limits

- kill tracking is still nearby-ped polling based
- loot tracking is still tied to observed looting state
- `RewardEarned` is still cash-delta based, not true contract-completion tracking
- perk/crafting/equipment effects are not yet fully hooked back into vanilla gameplay

## Tomorrow's Smallest Next Task

Split reward intake into a cleaner "contract-like payout" path so `Network` becomes less dependent on raw cash delta and more representative of true frontier contract progress.

## Additional Late Pass

- Stayed inside `E:\RDR2 RPG MODS\개발` and continued the native `ingame_mod` pass instead of touching the live game folder.
- Upgraded the live ledger from “Overview/Farming/Network/World only” into a broader live RPG bridge:
  - `Stats`
  - `Skills`
  - `Perks`
  - `Crafting`
  - `Armory`
  - `Ledger of the Dead`
  - `Collection`
  - `Gear`
- Added a conservative Deadeye bonus on top of the already working stamina and weapon-wear bonuses.
- Improved Korean HUD rendering by adjusting bitmap width/line-height/wrap budget and by feeding additional runtime Korean strings into the bitmap-font generator at build time.
- Fixed the workspace false-positive save-failure issue by touching session/profile files before write/flush and accepting a written non-empty file as a valid flush fallback.

### Additional Verification

- `.\build.ps1`
- `python -m unittest discover -s tests`
- `python -m frontier_rpg ui --check`
- `python .\scripts\workspace_smoke_test.py`
- `python .\scripts\workspace_smoke_test.py --keep-runtime`
- `.\preflight.ps1`

All of the above passed.

## Korean HUD Readability Pass

- Kept all work inside `E:\RDR2 RPG MODS\개발` and did not touch the live game folder for this pass.
- Reworked the native `ingame_mod` Korean HUD layout instead of adding more systems:
  - shortened Korean tab/page titles
  - shortened Korean live summaries and entry details
  - added a compact Korean objective line for the HUD footer and compact ledger
  - widened the compact/full ledger layout and reduced Korean text scale where needed
  - reduced entry capacity to values that actually fit the visible list panels
- Re-verified the pass with:
  - `.\build.ps1`
  - `python -m unittest discover -s tests`
  - `python -m frontier_rpg ui --check`
  - `python .\scripts\workspace_smoke_test.py`
  - `.\preflight.ps1`
- Result: build, tests, Korean smoke, corrupt-profile recovery, and preflight all passed after the layout/readability changes.

## Korean Renderer Hardening

- Identified the remaining Korean readability issue as a renderer problem, not a live-run / input / save problem.
- Reworked the bitmap font generator so glyphs now carry both a soft fringe mask and a solid stroke mask instead of a single hard black/white threshold.
- Increased the build-time Hangul sample size and lowered the soft threshold so Korean strokes survive small HUD rendering more cleanly.
- Added missing Korean HUD/runtime phrases to `runtime_bitmap_strings.txt` so the generated glyph atlas includes page-title, footer, and short-summary characters that were still falling back poorly.
- Re-ran:
  - `ingame_mod\\build.ps1`
  - `python -m unittest discover -s tests`
  - `python -m frontier_rpg ui --check`
  - `python .\\scripts\\workspace_smoke_test.py`
- All passed from the development workspace.

## Korean Root-Fix Pass

- Re-checked the actual generated bitmap header and confirmed the real renderer bottleneck was not dataset selection but Hangul row density in the HUD path.
- Rebuilt the Hangul bitmap pipeline around a smaller HUD-oriented font sample so the generated glyph header dropped to a 12-row layout instead of the denser earlier form.
- Added pixel snapping for bitmap draw runs and disabled the soft fringe on very small Korean text so thin strokes stop smearing together at HUD scale.
- Increased Korean glyph advance and widened the Korean-only compact/full ledger layout while reducing visible row count and wrap budget, so readable glyph size does not immediately cause panel overflow.
- Removed the Korean desktop-overlay fallback from the runtime decision path and reset the packaged INI back to `UseDesktopOverlay=0`, keeping the fix on the true in-game renderer instead of the failed external overlay path.
- Re-ran:
  - `ingame_mod\\build.ps1`
  - `python -m unittest discover -s tests`
  - `python -m frontier_rpg ui --check`
  - `python .\\scripts\\workspace_smoke_test.py`
  - `.\preflight.ps1`
- All passed from the development workspace after the root-fix pass.

## Zone Prompt Stabilization

- Investigated the in-field 안내문구 tremble and identified the most likely cause as `Beecher's Hope` zone-boundary jitter rather than ledger input/state failure.
- Added zone hysteresis to the native zone-state refresh so the prompt does not immediately drop on tiny coordinate swings near the boundary.
- Added a short grace window before the zone prompt is considered truly outside, and widened the release radius slightly for players who were already inside.
- Logged zone enter/exit transitions so the next live test can confirm whether prompt movement is caused by real zone transitions or purely visual issues.
- Shortened the Korean prompt text to `거점 [F11] 레저` and gave it a slightly larger prompt box so the hint is more stable and easier to read in-field.

## Korean Full-Tab Density Pass

- Confirmed the “only a few sections are visible” complaint was a real full-overlay layout issue, not missing page data.
- Rebuilt the Korean full-ledger tab strip into a two-row layout so more section titles can stay visible at once.
- Increased the Korean tab-header height and moved the content area down to preserve spacing after the second tab row.
- Kept the sliding page window and page indicator so the ledger still works with the full page set while exposing more of it on screen.

## Late UI Audit And Checklist

### Time Range Logged Today

- `17:19 ~ 17:30 KST`
  - clean reinstall prep against the live game root
  - Korean in-game visibility re-check after the renderer/layout passes
  - prompt readability review from the latest captured video
- `17:30 ~ 17:34 KST`
  - Beecher's Hope prompt jitter follow-up
  - zone hysteresis/grace-window verification
  - shorter Korean prompt copy review
- `17:34 ~ 17:35 KST`
  - Korean full-ledger tab-density follow-up
  - current UI bottleneck audit
  - implemented vs pending interaction checklist capture

### Current UI Bottlenecks To Fix First

- Korean renderer clarity is still the biggest blocker. The UI opens and updates, but Hangul readability is still below release quality.
- The field `F11` prompt can still feel unstable because prompt visibility and prompt readability are both part of the perceived flicker.
- The full ledger still needs better section exposure and panel balance so the page set does not look artificially small.
- The compact ledger needs cleaner density so it stops feeling half-empty when compared with the panel size.
- The stats/skills/perks pages need better visual clarity so the existing entry/detail structure is obvious on screen.

### Implemented State Right Now

- `F10` compact ledger opens in-game.
- `F11` full ledger opens in-game.
- Page switching works.
- Entry switching works.
- Session/profile save and restore work.
- Live counters update and feed the native ledger.
- `Overview`, `Farming`, `Network`, and `World` reflect live activity.
- `Stats`, `Skills`, `Perks`, `Crafting`, `Armory`, `Ledger of the Dead`, `Collection`, and `Gear` all exist in the native ledger.
- Entry labels plus detail-panel content exist for the stats/skills/perks family instead of being empty placeholders.

### Implemented But Still Not Clearly Visible

- Stat-entry selection state is implemented but Korean readability makes the movement hard to read in the current full-ledger presentation.
- Stat-entry detail text is implemented in the right-side detail panel but it is not yet visually obvious enough in Korean.
- The broader page set is present, but Korean tab density still makes it feel like fewer sections are available than actually exist.

### Still Not Implemented Yet

- Direct stat-point spending interaction
- direct perk unlock/confirm interaction
- direct crafting execution interaction
- accept/confirm-style action flow inside the native ledger

### Priority After This Audit

1. Finish Korean readability and prompt stability
2. make the existing entry/detail structure unmistakably visible
3. only then move on to direct interaction work for stats/perks/crafting

## Korean Readability + Detail Visibility Pass

- Pulled the active Frontier Ledger install back out of the real game root before starting this pass.
- Continued all work only from `E:\RDR2 RPG MODS\개발`.
- Reworked the Korean full-ledger layout so the UI hierarchy reads more clearly:
  - wider Korean full overlay
  - larger left entry-list panel
  - larger right detail panel
  - more readable bottom summary spacing
- Increased Korean tab width again by lowering the two-row strip from 5 tabs per row to 4 tabs per row.
- Reduced Korean visible entry capacity so the list no longer tries to show more rows than the enlarged row height can support cleanly.
- Added clearer panel hierarchy inside the full ledger:
  - `Entries`
  - `Summary`
  - `Selected`
  - `Detail`
  - `Current frontier`
- Strengthened selected-row visibility with a dedicated accent strip on the active entry row.
- Increased Korean text scale in the detail panel and entry list so stat/skill/perk details are easier to read when navigating with `Up/Down`.
- Slightly widened Hangul advance at draw time and regenerated the bitmap font with a larger build-time font size plus a lower solid threshold so Korean strokes separate more cleanly.

### Verification After This Pass

- `.\build.ps1`
- `python -m unittest discover -s tests`
- `python -m frontier_rpg ui --check`
- `python .\scripts\workspace_smoke_test.py`
- `.\preflight.ps1`

All passed from the development folder after the readability/detail visibility pass.

## Korean Renderer Strategy Swap

- Confirmed the underlying failure was no longer data or UI state, but the old Korean renderer itself:
  - panel rectangles were stable
  - ledger state changes were stable
  - the Hangul-heavy full UI was still collapsing visually because too much Korean text was going through the bitmap path
- Reworked the Korean render strategy instead of only tuning the same page browser again.
- Added an ASCII fast path so numeric / English-safe value lines in the Korean runtime now use the stable native text path instead of the bitmap Hangul path.
- Replaced the Korean full ledger presentation with a simpler dashboard-style layout focused on:
  - `개요`
  - `진행`
  - `전투`
  - `사냥`
  - `능력`
  - `기술`
  - `제작`
  - `보너스`
  - `현재`
- Replaced the Korean compact ledger with a simpler dashboard summary so `F10` also uses fewer Hangul-heavy wrapped blocks.
- Kept the live counters, progression data, and bonuses intact, but changed the Korean UI to show them through short headings plus ASCII metrics instead of long wrapped Korean detail lines.

### Verification After Renderer Swap

- `.\build.ps1`
- `python -m unittest discover -s tests`
- `python -m frontier_rpg ui --check`
- `python .\scripts\workspace_smoke_test.py`
- `.\preflight.ps1`

All passed from `E:\RDR2 RPG MODS\개발`.
